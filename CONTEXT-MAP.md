# Context Map

This repo is organized as a multi-package monorepo. Each context maintains its own domain model and architectural decisions.

## Contexts

- **`mtng/`** — [one-line description of this context]
- **`packages/`** — [one-line description of this context]
- **`apps/`** — [one-line description of this context]
- **`deploy/`** — [one-line description of this context]

For detailed domain terminology and architectural decisions, see each context's `CONTEXT.md` and `docs/adr/` directory.

## System-wide decisions

See `spec/adr/` for decisions that span multiple contexts.
