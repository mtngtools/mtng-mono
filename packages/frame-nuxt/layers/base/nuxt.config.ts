// Base layer: shared defaults for mtngTOOLS Nuxt apps.
// Use in app: extends: ['@mtngtools/frame-nuxt/layers/base']
import { defineNuxtConfig } from 'nuxt/config';

export default defineNuxtConfig({
  // Add base-layer defaults here (e.g. meta, app config).
});
