# Copy to `.env` in this directory for MOCK_DATA_* only.
# Shared AWS_* (e.g. AWS_REGION) — set once in mtng-mono repo root `.env`.
# See ../../../spec/env/README.md and ../../../.env.example

MOCK_DATA_S3_BUCKET_NAME=my-org-mock-data-dev
MOCK_DATA_S3_BUCKET_KEY_PREFIX=mock-data-prefix/
