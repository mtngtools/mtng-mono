# AWS SDK default chain (optional static keys; prefer SSO/profile when possible).
# @defaultRequired=true
# @defaultSensitive=false
# ---
# @optional
AWS_REGION=
# @optional @sensitive
AWS_ACCESS_KEY_ID=
# @optional @sensitive
AWS_SECRET_ACCESS_KEY=
# @optional @sensitive
AWS_SESSION_TOKEN=
