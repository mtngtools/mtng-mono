# Timeline & steps: session

Session-specific detail for the [timeline & steps](../README.md) pattern.

## Scope

- Extract the "Session example" section currently inline in [`../README.md`](../README.md) (timeline: unknown → pre-live → live → post-live → archive → retired; CTA steps) into this dedicated doc
- Not yet extracted — treat `../README.md` as the source of truth until this happens
