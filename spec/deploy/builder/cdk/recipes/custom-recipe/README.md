# Recipe Spec: `addCustomRecipe`

Defines requirements for user-defined recipes built on builder primitives.

## Scope

- Required custom recipe interface and typing contract
- Recipe context capabilities exposed to custom recipes
- Fluent behavior (`addCustomRecipe` returns `this`)
- Typed refs expectations and deterministic `refs(recipeKey)` behavior
- Validation and discovery requirements for resources registered by custom recipes
