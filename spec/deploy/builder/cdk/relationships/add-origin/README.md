# Relationship Spec: `addOrigin`

Defines requirements for attaching origin resources to a distribution.

## Scope

- Supported origin kinds (for example, bucket/function/API origin adapters)
- Behavior mapping and path pattern expectations
- Validation for unsupported source/target combinations
- Guidance for preferring constructor-prop composition when CDK already supports it
