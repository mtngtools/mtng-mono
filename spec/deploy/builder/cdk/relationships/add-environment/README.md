# Relationship Spec: `addEnvironment`

Defines requirements for injecting environment variables into function-like resources.

## Scope

- Supported target kinds
- Value source options (literal values, refs, computed values)
- Name conflict and override behavior
- Validation for unresolved refs
