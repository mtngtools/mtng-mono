{
  "name": "@mtngtools/mono",
  "version": "0.0.0",
  "private": true,
  "description": "mtngTOOLS monorepo — shared packages for medical conference video platforms",
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:e2e": "turbo run test:e2e",
    "clean": "turbo run clean",
    "playwright:install": "PLAYWRIGHT_BROWSERS_PATH=\"$PWD/node_modules/.pnpm/playwright-core@1.58.2/node_modules/playwright-core/.local-browsers\" playwright install chromium",
    "changeset": "changeset",
    "prepublishOnly": "pnpm run typecheck && pnpm run lint && pnpm run build && pnpm run test"
  },
  "keywords": ["mtngtools", "monorepo"],
  "author": "mtngtools",
  "license": "MIT",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "playwright": "^1.58.2",
    "turbo": "^2.0.0",
    "typescript": "^5.7.0"
  }
}
